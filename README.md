# Pytorch_Performance_Prediction_neural_network
A PyTorch-based regression project that predicts employee performance scores using a custom neural network. The project demonstrates data preprocessing, model building, training, evaluation, and CPU/GPU execution using a synthetic dataset.
